<!DOCTYPE html>
<html>
  <head>
    <title>ChatAi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="shortcut icon" type="image/png" href="<%= asset_path('logo.svg') %>"/>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <header>
      <nav class="navbar">
        <img src="<%= asset_path('logo.svg') %>" alt="Logo" class="logo">
        <ul>
          <li><%= link_to 'Chat', chat_index_path %></li>
          <li><%= link_to 'Variações de Imagem', image_variants_path %></li>
          <li><%= link_to 'Geração de Imagem', image_generation_index_path %></li>
        </ul>
      </nav>
    </header>
    <main>
      <%= yield %>
    </main>
    <div id="loading" style="display: none">
      <div class="lds-roller"><div></div><div></div><div></div><div></div><div></div>
    </div>
  </body>
</html>
